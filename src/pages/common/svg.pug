//- mixin icon(name, url)
//-   - mod = mod || ''
//-     a(href="#{url}" class="#{name}_link")
//-       svg(class="icon icon-" + name)
//-         use(xlink:href="/build/img/sprite.svg#" + name)
- var svgSprite = 'img/sprite.svg'
mixin icon(name, modClass)
  if modClass == undefined
    - var modClass = ''
  else
    - var modClass = ' ' + modClass
  svg(class="icon icon-" + name + modClass)
    use(xlink:href= svgSprite + '#' + name)
